# Wave 2 close brief — P1 banking-core fan-out, width 3 (2026-09-01)

**Landed.** Three units in parallel, one child each, all merged via `migration/04-wave2-integration` into `migration/02-analysis-plan`: PR #31 `daily_transaction_processing` (U2 → `sas_silver.daily_transactions/running_balances/txn_anomalies/txn_rejected`), PR #30 `credit_risk_scoring` (U3 → `sas_silver.risk_scores/risk_migration`, `sas_gold.risk_summary`), PR #32 `monthly_regulatory_reporting` (U4 → `sas_gold.monthly_rwa/delinquency_aging/llp_coverage/capital_adequacy` + `REG_REPORT_202401.xlsx`, 3 sheets). Independent wave recon PR #33 (`docs/migration/ts-sas-legacy-analytics_P1_wave2_recon.md`): U2 64/0, U3 96/0, U4 70/0 PASS/FAIL; all 11 target row counts equal the reference (18903/610/46/12; 236/195/12; 59/70/6/1); manifest sha `aea7c04a…25cc7`; mode DEGRADED, reference-derived, not SAS-produced. Each child used 1 live recon run of cap 3 (pilot used 3) and proved idempotency across 2-3 runs. Exceptions: 0.

**Decided.** DEC-015 (a) applied literally throughout: `txn_rejected` = 10 feed columns, `daily_transactions` keeps the 10-column base schema, `monthly_rwa` key includes `risk_weight`, pre-append anomaly statistics, sample std, missing-low ordering. Zero-denominator `coverage_pct` kept as literal `0` (source `else 0`), capital ratios `else .` → NULL. Capital constants 50M/65M/80M reproduced verbatim as PLACEHOLDERS (not corrected). No tolerance changed. New **DEC-017 (PROPOSED)**: `%parmv` upper-cases `model_id`, so literal SAS writes `MODEL_ID='CRM-2023-Q4-V2'` while reference and conversion emit `…-v2`; recommended (a) correct both at wave 3.

**Broke / implies.** (1) Concurrent `bundle deploy/run` by parallel children overwrote each other at `/Workspace/.bundle/sas_legacy/dev/files` — one B2 job run executed B3's placeholder. No data collision; runs re-done via the warehouse executor. Wave 3 is width 1 and the coexistence job is parent-owned, so serialised bundle ownership is the rule from here. (2) Spark numeric literals are DECIMAL; the scorer needed `…D` literals to hold ML-2. (3) `openpyxl` cannot save onto a FUSE Volume; write local then copy (fixed in shared `export_xlsx.py`). (4) Recon T-12 opens `--xlsx-path` locally, so the job's real Volume path fails spuriously; harness fix due before the wave-3 window. (5) Warehouse-CLI xlsx cells are strings, serverless cells are typed — workbook contents stay outside the row-level gate.

**Unproven / open.** T-9 DECLARED-UNEXERCISED on `txn_rejected` (and `acct_exceptions`) until REQ-05. Seed does not exercise: missing FICO, missing secured LTV, missing MTG LTV `else 1.00`, `Unknown` delinquency bucket, missing PROC MEANS class values. Capital constants placeholder (high, owner requester, gate STOP E). DEC-016 (append vs overwrite for exception tables) and DEC-017 (MODEL_ID case) both PROPOSED, decide before STOP E. GitHub Actions still produce no check runs; gates (ruff, 63 tests, bundle validate, live recon) verified locally and by the independent session.

**Cost.** Children: B4 9.1 ACU (B2/B3 per session ledgers, same order); independent recon 3.8 ACU. Warehouse `565cd2fd713738c4`: ~105 short statements across children + ~40 for the independent window (≈9 s of query time per unit). Serverless job runs: B3 1 SUCCESS; B4 2 (1 FAILED on FUSE save, 1 SUCCESS); B2 0 successful bundle runs (warehouse executor). No clusters. Live recon runs 4 of 12 allowed.
