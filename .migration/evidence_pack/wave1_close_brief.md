# Wave 1 close brief — P1 banking-core pilot (2026-09-01)

**Landed.** PR #29 (W1 B1 / U1 `load_customer_accounts` → `databricks/src/jobs/load_customer_accounts.py`, Spark SQL, params `business_date`/`region`) merged into `migration/02-analysis-plan`. Live recon U1 2024-01-31 **PASS**: 52 PASS / 0 FAIL / 2 N/A / 1 DECLARED-UNEXERCISED (`cust_accounts_daily` 466/466, `acct_exceptions` 32/32); mode DEGRADED, "reference-derived, not SAS-produced", manifest sha `aea7c04a…625cc7`; report `databricks/evidence/w1_b1/recon.{json,summary.md}`. Idempotency: CLI run and serverless job run `636052434694604` give identical counts/checksums (`466 / -36758330745`, `32 / -1237428266`). Source volume before run: 487 / 250 = manifest. ruff / pytest 30 / bundle validate pass locally (orchestrator re-ran ruff + pytest on the merged tree: pass). Job `sas_legacy_run_daily_banking` stays PAUSED; serverless only; no legacy source or reference file touched (diff against `Config/ Formats/ Macro/ Programs/ BatchJobs/ Data/ docs/migration/recon/reference` is empty). Exceptions in this wave: 0 FAIL.

**Decided.** DEC-015 (a) applied literally: `acct_exceptions` = 29 columns (DROP applies to both outputs, AMB-01); rows OUTPUT before `SNAPSHOT_DATE`/`LOAD_TIMESTAMP` assignment so both are NULL on every exception row (AMB-05) — the U1 spec therefore classifies those two columns T-3 (exact null) on that table rather than T-7; `. < 0` TRUE for NEG_BAL. Harness (promote-once): `TableSpec(multiset, distinct_keys, t9_unexercised)`, T-9 verdict label `DECLARED-UNEXERCISED` with the DEC-015 citation, multiset keys no longer forced to T-3 (§3 rules govern). No tolerance value changed; no other unit spec touched.

**Broke / implies.** Three live recon runs (cap 3) were consumed by harness-spec classification only (T-8 `interest_rate` T-3-vs-T-5; T-7 on literally-null columns) — converted code unchanged across runs; the cap should be read as "harness + code" and wave-2 hand-offs must pre-state the column classes for multiset tables. Serverless `spark_python_task` has no `__file__` and treats `SystemExit(0)` as FAILED with auto-retry (one wasted job run + retry); non-stdlib imports need `environments[].spec.dependencies` (`bundle validate` does not catch it). `sas-programs` skill directory absent from the plugin. All promoted to the wave-2 preflight.

**Unproven / open.** (1) T-9 on `acct_exceptions` DECLARED-UNEXERCISED until REQ-05 / production schema (owner requester, medium, gate STOP E). (2) **Exception-history semantics (proposed DEC-016)**: legacy `insert into STG_BANK.ACCT_EXCEPTIONS` appends across runs; the literal schema has no date discriminator (snapshot_date NULL), so the conversion overwrites the table wholesale to stay idempotent. Parity for one business date is exact; multi-day accumulation is not reproduced. Recommended: keep overwrite through wave 3; decide append-vs-overwrite (or a non-literal partition column) at coexistence before STOP E. (3) GitHub Actions still produce no check runs (gates verified locally). (4) Recon remains reference-derived.

**Cost.** W1 B1 child 15.8 ACU; ~20 short warehouse statements; 3 ad hoc serverless job runs (1 SUCCESS, 1 FAILED-on-exit-code incl. auto-retry, 1 failed pre-write); 2 bundle deploys; 3 live recon runs; 0 clusters. Orchestrator ≈ 1.5 ACU this wave.
