# SKILL FEEDBACK harvest (per wave)

Systematic findings reported by children. Status: OPEN until promoted into the plugin skill (external repo `dbx-migration-plugin`) or into a child hand-off preflight line.

| Wave | Skill | Finding | Promotion |
|---|---|---|---|
| 0 | databricks-auth-cli | Pre-set `DATABRICKS_HOST/TOKEN` in the env shadow the `DATABRICKS_DEMO_*` names; `bundle validate` 403s until the mapping is exported explicitly. | Added to every P1 hand-off preflight (wave 1+). Skill PR: OPEN. |
| 0 | unity-catalog-conventions | `databricks fs cp` needs `dbfs:/Volumes/...`; SQL uses `/Volumes/...`. | Hand-off preflight (wave 1+). Skill PR: OPEN. |
| 0 | data-reconciliation | No fixture-mode pattern when reference outputs do not exist yet; harness now exits 2 "reference missing". | Resolved by W0-R landing; pattern noted. Skill PR: OPEN. |
| 0 | data-reconciliation | DROP/KEEP *statements* apply to every OUTPUT set of a multi-output DATA step; only `(drop=)` data-set options are per-target. `PROC APPEND FORCE` truncates to BASE schema. | Hand-off for U1/U2 (wave 1-2). Skill PR: OPEN. |
| 0 | data-reconciliation | SAS `. < x` is TRUE in DATA-step/CASE logic (not only sort order); classification chains diverge from Spark NULL semantics. Check declared keys against the SAS GROUP BY. | Hand-off for U2/U4. Skill PR: OPEN. |
| 0 | prediction-parity | PROC MEANS `N=` without var list = non-missing count of first VAR, not `_FREQ_`; `std` is sample (n-1); anomaly stats from pre-append history. | Hand-off for U2/U3. Skill PR: OPEN. |
| 1 | asset-bundles / sas-programs | Serverless `spark_python_task` exec()s the file: no `__file__`; any `SystemExit` (even 0) is reported FAILED and auto-retried. Resolve paths via `sys.argv[0]` fallback; never `raise SystemExit(0)`. | Wave-2 hand-off preflight. Skill PR: OPEN. |
| 1 | asset-bundles | Non-stdlib imports (e.g. `openpyxl` via `sas_macros`) need `environments[].spec.dependencies`; `bundle validate` does not catch the omission. | Wave-2 hand-off preflight. Skill PR: OPEN. |
| 1 | data-reconciliation | Statement Execution API DDL results carry no `manifest.schema`; harness must not assume result columns (fixed in `recon/warehouse.py`). | Fixed in harness (PR #29). Skill PR: OPEN. |
| 1 | data-reconciliation | Full-row multiset tables: pre-state per-column rule classes in the hand-off (T-3 vs T-5 numerics, literally-null run-time columns = T-3 not T-7) or the live-recon cap is spent on harness spec, not code. | Wave-2 hand-off (U2 `txn_rejected`). |
| 1 | sas-programs | Skill directory absent under the plugin's `skills/`; children derive DATA-step semantics from the hand-off preflight only. | Owner plugin maintainers. OPEN. |
| 1 | asset-bundles / sas-programs | UC Volumes are FUSE-mounted without random-access writes: `openpyxl` `Workbook.save()` (zip seek) straight onto `/Volumes/...` fails with `OSError [Errno 5]` on serverless. Build the file in a local temp dir and `shutil.copyfile` (sequential) to the volume; CLI path uses Files API `PUT /api/2.0/fs/files/...?overwrite=true`. | Fixed in U4 `run()`/`_publish_local` (W2 B4 PR). Skill PR: OPEN. |
| 2 | asset-bundles | Concurrent wave children `bundle deploy`/`bundle run` against the same target overwrite each other's files at `/Workspace/.bundle/sas_legacy/dev/files`; a B2 job run executed B3's placeholder. Serialize deploy/run per wave (one bundle owner) or run ad hoc via the warehouse executor; keep `max_concurrent_runs: 1` (sibling runs queue rather than collide). | Wave-3 hand-off preflight (single child, no contention); coexistence job owner = parent. Skill PR: OPEN. |
| 2 | prediction-parity | Spark SQL numeric literals (`0.412`) are DECIMAL; scorecard arithmetic silently becomes DECIMAL unless every constant is a DOUBLE literal (`0.412D`) — otherwise ML-2 (abs <= 1e-9) is at risk after `exp()`. | Hand-off preflight for any scorer unit. Skill PR: OPEN. |
| 2 | sas-programs | `%parmv` defaults to `_CASE=U`: every macro parameter routed through `%parmv` is upper-cased at runtime (`model_id=CRM-2023-Q4-v2` -> `CRM-2023-Q4-V2` in `MODEL_ID`). The reference emitted the mixed-case literal; see DEC-017. | Reference-build checklist: apply `%parmv` case rules before emitting string constants. Skill PR: OPEN. |
| 2 | data-reconciliation | Fixture-first iteration held the live cap at 1 run per child in wave 2 (vs 3 in the pilot) once per-column rule classes were pre-stated in the hand-off; keep the pattern. Unexercised-by-seed branches (missing FICO, missing secured LTV, missing MTG LTV `else 1.00`, `Unknown` delinquency bucket) must be listed as DECLARED-UNEXERCISED, not PASS. | Promote to skill as the default child loop. Skill PR: OPEN. |
| 2 | data-reconciliation | Statement Execution API JSON_ARRAY returns strings, so a warehouse-CLI path writes string xlsx cells while the serverless path writes typed cells; keep workbook contents outside the row-level gate (T-12 = existence + sheet names). | Recorded in U4 spec. Skill PR: OPEN. |
| 2 | data-reconciliation | `recon/rules.py::t12` opens `--xlsx-path` with local `openpyxl.load_workbook`, so the job's real `/Volumes/...` output path yields a spurious T-12 FAIL; a read-only `databricks fs cp` to a local file is required, and `recon.summary.md` does not record which path/sha was checked. Fix: fetch Volume paths via the Files API and record source object + sha in `recon.json`. | Harness fix due before the wave-3 window (parent-owned; not a tolerance change). OPEN. |
